<%= react_component("pages/CustomerConsentNew", {
    csrfToken: form_authenticity_token,
    uuid: @user.uuid,
    logo_src: image_url('logo.png'),
    button_src: image_url('login_button.svg'),
    pending_customer_user: @pending_customer_user,
    invited_by_name: @user.invited_by&.name,
    customer: @pending_customer_user&.customer
}) %>